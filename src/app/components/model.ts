export interface itinerary {
    iconName: string;
    title: string;
    subtitle: string;
    img: string;
    description: string;
}

export interface content {
    iconName: string;
    label: string;
    details: string;
}

export interface navigation {
    link: string;
    name: string;
}